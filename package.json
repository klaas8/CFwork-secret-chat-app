{
  "devDependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0",
    "esbuild": "^0.25.12",
    "miniflare": "^4.20251011.2",
    "wrangler": "^4.45.4"
  },
  "name": "cfwork-secret-chat-app-main",
  "version": "1.0.0",
  "description": "利用CFwork匿名在线聊天（他更像一个群组，你可以在里面进行聊天）",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --bundle --outfile=dist/index.js --format=esm",
    "dev": "npm run build && node dev.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
