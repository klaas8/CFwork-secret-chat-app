# wrangler.toml (最终版)

name = "cfwork-secret-chat-app"
main = "src/index.js" # 我们的主代码文件
compatibility_date = "2024-03-01"

# 【【【 关键部分 】】】
# 直接在这里定义 Durable Object 绑定
# 这样部署时，如果命名空间不存在，它会自动创建！
[[durable_objects.bindings]]
name = "CHAT_ROOM"
class_name = "ChatRoom"
